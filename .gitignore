node_modules/
dist/
coverage/
.wrangler/
.DS_Store
.env
.env.*

# The rendered résumés are build ARTEFACTS, not source.
# Versioning them would pull a binary into the history for every comma fixed,
# and would create a second source of truth for a file the build knows how to
# reproduce. They are produced by `npm run build:cv`, and published alongside
# the Worker as static assets.
public/cv/
